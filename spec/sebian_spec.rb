# frozen_string_literal: true

RSpec.describe Sebian do
  it "has a version number" do
    expect(Sebian::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
